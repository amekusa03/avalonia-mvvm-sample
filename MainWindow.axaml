<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="using:SampleApp"
        mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="200"
        x:Class="SampleApp.MainWindow"
        x:DataType="local:MainWindowViewModel"
        Title="SampleApp2"
        Width="400" Height="200">

  <!-- StackPanel to arrange controls vertically -->
  <StackPanel Spacing="10" Margin="20">
    <TextBlock Text="名前を入力してください:" />

    <!-- Bind Text property to UserName property in ViewModel -->
    <TextBox Text="{Binding UserName, Mode=TwoWay}" />

    <!-- Bind Command property to GreetCommand in ViewModel -->
    <Button Content="挨拶する" Command="{Binding GreetCommand}" />

    <!-- Bind Text property to Greeting property in ViewModel -->
    <TextBlock Text="{Binding Greeting}" FontSize="16" HorizontalAlignment="Center" Margin="0,10,0,0" />
  </StackPanel>

</Window>
